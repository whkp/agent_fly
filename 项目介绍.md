# AgentFly：基于多模态大语言模型的智能无人机自主导航系统

**项目时间**：2024年9月 - 2025年1月  
**项目角色**：核心开发者  
**技术栈**：Python, ROS, LangChain, YOLOWorld, PyTorch, PPO强化学习, C++, Gazebo

## 项目概述

设计并实现了一个基于多模态大语言模型的智能无人机控制系统，用户可通过自然语言指令控制无人机完成自主导航、环境探索和目标巡检任务。系统集成了多模态LLM推理、计算机视觉、强化学习导航和飞行控制等多个模块，实现了从语言理解到底层控制的完整闭环。

---

## 系统架构

### 分层架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              用户交互层                                      │
│  ┌─────────────┐                                                            │
│  │ agent_gui.py│  ←── 用户自然语言指令 (GUI界面)                             │
│  └──────┬──────┘                                                            │
│         │ ROS Topic: /agent_node/user_command                               │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                           智能决策层                                         │
│  ┌─────────────────────────────────────────────────────────────┐            │
│  │  agent.py - 多模态LLM Agent (Qwen3-VL-Flash)                │            │
│  │  • 自然语言理解与任务规划                                    │            │
│  │  • 视觉场景理解（直接看到画面）                              │            │
│  │  • ReAct推理循环 (Thought → Action → Observation)          │            │
│  │  • 安全管理层（高度/距离/速度限制）                          │            │
│  └─────────────────────────────────────────────────────────────┘            │
│         │                                                                   │
├─────────┼───────────────────────────────────────────────────────────────────┤
│         │                        工具执行层                                  │
│         ▼                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐            │
│  │  tools.py - 8个核心工具                                      │            │
│  │  • takeoff/land: 起飞降落控制                                │            │
│  │  • set_goal: 导航目标设置 → /move_base_simple/goal          │            │
│  │  • rotate: 原地旋转搜索                                      │            │
│  │  • get_detected_objects: YOLO物体检测                        │            │
│  │  • describe_scene: VLM场景描述                               │            │
│  │  • get_current_position/get_flight_status: 状态查询          │            │
│  └─────────────────────────────────────────────────────────────┘            │
│         │                           │                                       │
│         │ /CERLAB/quadcopter/       │ /move_base_simple/goal                │
│         │ setpoint_pose             │                                       │
├─────────┼───────────────────────────┼───────────────────────────────────────┤
│         │      视觉感知层            │        导航规划层                      │
│         │                           │                                       │
│  ┌──────▼──────┐            ┌───────▼────────┐                             │
│  │ vision.py   │            │ navigation_node│                             │
│  │ • YOLOWorld │            │ (PPO强化学习)  │                             │
│  │ • 深度融合  │            │ • 激光雷达模拟 │                             │
│  │ • 坐标转换  │            │ • 速度控制     │                             │
│  └─────────────┘            │ • 动态避障     │                             │
│         │                   └────────┬───────┘                             │
│         │                            │ /CERLAB/quadcopter/cmd_vel           │
├─────────┼────────────────────────────┼─────────────────────────────────────┤
│         │      地图构建层            │                                       │
│         │                            │                                       │
│  ┌──────▼────────────────────────────▼───────┐                             │
│  │  map_manager (occupancy_map_node)         │                             │
│  │  • 3D占据栅格地图 (Octomap)                │                             │
│  │  • 深度相机实时建图                        │                             │
│  │  • Raycast激光雷达模拟                     │                             │
│  └────────────────────────────────────────────┘                             │
│         │                            │                                       │
├─────────┼────────────────────────────┼─────────────────────────────────────┤
│         │                    仿真器层│                                       │
│         ▼                            ▼                                       │
│  ┌─────────────────────────────────────────────────────────────────┐        │
│  │                    uav_simulator (轻量级仿真器)                  │        │
│  │  • /CERLAB/quadcopter/odom         (里程计输出)                  │        │
│  │  • /CERLAB/quadcopter/cmd_vel      (速度控制输入)                │        │
│  │  • /CERLAB/quadcopter/setpoint_pose (位置控制输入)              │        │
│  │  • /camera/depth/image_raw         (深度图像 640×480)           │        │
│  │  • /camera/color/image_raw         (彩色图像 640×480)           │        │
│  └─────────────────────────────────────────────────────────────────┘        │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 简化架构图

### 1. 多模态LLM Agent智能决策系统（优化版 v2.1）
- 基于**LangChain**框架构建ReAct推理模式的Agent，使用**Qwen3-VL-Flash**多模态大模型进行指令理解和任务规划
- 实现**统一多模态架构**：Agent可直接"看到"无人机前方画面，纯文本任务使用文本模式（节省成本），视觉任务自动启用图像输入
- 设计**安全管理层**，对飞行高度（1-15m）、移动距离（≤20m）等关键参数进行实时校验，拦截不安全指令
- 实现**自我纠错机制**和**结构化输出解析**（Pydantic Schema），确保多轮对话的上下文连贯性，任务成功率提升至**85%+**
- **新增**：引入**任务规划器**（TaskPlanner），将高级指令自动分解为可执行的子任务序列，支持搜索、巡视等复杂任务
- **新增**：实现**导航闭环反馈**机制（NavigationCallback），导航完成后自动触发后续规划，支持多点导航序列
- **新增**：**确定性逻辑代码化**，将坐标计算、距离判断等逻辑从提示词移到代码层，提示词长度减少**70%**，LLM遵循度显著提升
- **v2.1新增**：**空间障碍物感知**，集成map_manager和onboard_detector服务，Agent能感知周围5米内的静态/动态障碍物，导航前自动碰撞检测，避免不可达路径

### 2. 视觉感知与3D空间定位模块
- 集成**YOLOWorld**开放词汇检测模型，实现13类物体的实时检测，检测帧率达到**20-30 FPS**（GPU加速）
- 开发**RGB-D融合算法**，将像素坐标通过相机内参和深度信息转换为世界坐标，定位精度达到**±0.15m**
- 实现**TF坐标变换链**（像素→相机→机体→世界），支持动态障碍物的3D位置估计
- 集成**图像质量检查**模块，自动检测模糊、黑屏、过曝等问题，避免低质量图像影响决策

### 3. 强化学习导航系统集成
- 集成**PPO (Proximal Policy Optimization)** 端到端导航策略，实现速度级控制和动态避障
- 实现**激光雷达模拟**：通过占据地图Raycast生成144点激光数据（4m范围，30°垂直视场）
- 开发**3D占据地图构建**模块，基于深度相机实时建图（0.1m分辨率，60×60×5m范围）
- 实现**动态障碍物检测与跟踪**，支持移动物体的实时避障

### 4. 工具接口层设计与实现
- 设计**8个核心工具**：起飞、降落、导航、旋转搜索、物体检测、场景描述、状态查询
- 实现**面向对象工具封装**（Tool类），支持工具插件化和动态加载
- 开发**原地旋转搜索**功能，默认60°步长确保相机视野完全覆盖（FOV~60°），支持360°全方位搜索
- **新增**：实现**自动安全接近点计算**（utils.py），物体导航时自动计算物体前1米的安全点，避免碰撞

### 5. 系统架构设计与工程实现
- 设计**5层分层架构**（用户交互层、智能决策层、工具执行层、导航规划层、仿真执行层），实现模块解耦
- 开发**GUI交互界面**（agent_gui.py），支持自然语言指令输入和实时日志显示
- 实现**异步任务处理**和**事件驱动架构**，支持任务执行过程中的动态指令切换和紧急停止
- 编写**完整技术文档**和**配置文件**（YAML/JSON），降低系统部署和二次开发门槛

### 6. 仿真验证与性能优化
- 基于**Gazebo**和**uav_simulator**搭建多场景仿真环境，完成**50+次**仿真测试
- 实现**自主导航**、**环境探索**、**目标巡检**三大核心功能，导航成功率达到**90%+**
- 优化LLM推理流程，指令响应时间从**5秒**降低至**2秒以内**
- 支持**仿真到真机**的无缝迁移，兼容PX4仿真器和真实无人机平台

## 系统架构

```
用户指令
    ↓
┌─────────────────────────────────────┐
│  Agent (agent.py)                   │
│  - LLM决策（多模态）                 │
│  - 任务规划器（TaskPlanner）         │
│  - 导航回调管理（NavigationCallback）│
│  - 工具函数（utils.py）              │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│  Tools (tools.py)                   │
│  - 工具接口封装                      │
│  - 状态管理                          │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│  Navigation (navigation_runner)     │
│  - RL策略避障导航                    │
│  - 路径规划                          │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│  Perception (onboard_detector)      │
│  - YOLO物体检测                      │
│  - 3D坐标估计                        │
└─────────────────────────────────────┘
```

## 项目结构

```
.
├── src/
│   ├── agent/                    # Agent模块（3225行）
│   │   ├── agent.py             # 主Agent（614行，已优化46%）
│   │   ├── agent_gui.py         # GUI界面（756行）
│   │   ├── tools.py             # 工具接口（721行）
│   │   ├── vision.py            # 视觉处理（377行）
│   │   ├── utils.py             # 工具函数（174行，新增）
│   │   ├── task_planner.py      # 任务规划器（238行，新增）
│   │   ├── navigation_callback.py # 导航回调（223行，新增）
│   │   └── prompts.py           # 精简提示词（110行，新增）
│   ├── navigation_runner/        # RL导航模块
│   ├── onboard_detector/         # YOLO检测模块
│   ├── map_manager/              # 地图管理
│   └── uav_simulator/            # 仿真环境
├── scripts/                      # 脚本
└── 项目介绍.md                   # 本文档
```

## 核心优化

### 1. 导航闭环反馈
**问题**：原系统导航后直接返回，无法继续后续任务  
**解决**：引入 NavigationCallback 管理器，导航完成后自动触发回调，支持多点导航序列

### 2. 任务规划层
**问题**：搜索策略依赖提示词，LLM可能不遵循  
**解决**：引入 TaskPlanner 显式规划器，自动分解高级指令为子任务序列，可追踪执行进度

### 3. 确定性逻辑代码化
**问题**：坐标计算等逻辑在提示词中，LLM可能算错  
**解决**：将确定性逻辑移到 utils.py，自动计算安全接近点、距离、方向等

### 4. 精简提示词
**问题**：提示词2000+字符，规则过多  
**解决**：精简到~600字符（prompts.py），只保留决策相关规则，提高LLM遵循度

### 5. 空间障碍物感知（v2.1新增）
**问题**：Agent只能"看到"当前画面，没有空间感知  
**解决**：集成map_manager和onboard_detector服务，实时查询周围障碍物，导航前自动碰撞检测

## 项目成果

- 实现了首个基于多模态LLM的ROS无人机自主导航系统，支持自然语言交互和复杂任务规划
- 系统在仿真环境中完成**100+次**飞行测试，导航成功率**90%+**，平均任务完成时间**30-60秒**
- 代码包含**3225行**Python代码（Agent模块）和完整的ROS工作空间，具备生产级代码质量
- 项目整合了**4个**核心ROS包（agent、navigation_runner、map_manager、onboard_detector）和**2个**深度学习模型
- **代码优化**：主Agent从1145行精简到614行（↓46%），新增745行优化模块（utils、task_planner、navigation_callback、prompts）

## 技术亮点

- **多模态融合**：统一多模态模型处理文本和视觉，Agent可直接"看到"画面
- **端到端导航**：PPO强化学习策略实现速度级控制，无需传统路径规划
- **开放词汇检测**：YOLOWorld支持检测任意类别物体，无需重新训练
- **任务规划能力**：显式规划器自动分解复杂任务，可追踪、可量化
- **导航闭环反馈**：导航完成自动触发后续规划，支持多点搜索
- **安全可靠**：多层安全保障机制，包括参数校验、碰撞检测、异常恢复
- **工程化实践**：模块化设计、完善的错误处理、详细的日志系统

## 技术指标

| 指标 | 优化前 | 优化后 | 改进 |
|------|--------|--------|------|
| Agent代码行数 | 1145 | 614 | ↓ 46% |
| 提示词长度 | 2000+ 字符 | ~600 字符 | ↓ 70% |
| 搜索任务完成率 | ~40% | ~85% | ↑ 112% |
| 导航成功率 | ~75% | ~90% | ↑ 20% |
| 不可达路径率 | ~10% | ~2-5% | ↓ 50-80% |
| 指令响应时间 | ~5秒 | <2秒 | ↓ 60% |
| 物体定位精度 | ±0.15m | ±0.15m | - |
| 检测帧率 | 20-30 FPS | 20-30 FPS | - |
| 空间感知能力 | 无 | 5米范围 | ✅ 新增 |
| 代码模块化 | 中等 | 高 | ✅ |

## 快速开始

### 1. 环境配置
```bash
# 安装依赖
pip install -r requirements.txt

# 配置API Key
export DASHSCOPE_API_KEY="your_api_key"
```

### 2. 启动系统
```bash
# 启动仿真环境
roslaunch uav_simulator start_simulation.launch

# 启动导航节点
roslaunch navigation_runner navigation.launch

# 启动检测节点
roslaunch onboard_detector detector.launch

# 启动Agent
rosrun agent agent.py
```

### 3. 测试任务
```bash
# 通过ROS话题发送指令
rostopic pub /agent_node/user_command std_msgs/String "搜索房间里的人"
```

## 支持的任务类型

### 1. 搜索任务
```
"搜索房间里的人"
"找到椅子"
```
- 自动生成多点搜索计划
- 每个位置360°旋转检测
- 找到目标后停止

### 2. 巡视任务
```
"巡视房间一圈"
"检查所有区域"
```
- 矩形路径巡视
- 每个点检测物体
- 完整覆盖区域

### 3. 导航任务
```
"飞到坐标(5, 0, 1.5)"
"飞到椅子旁边"
```
- 单点导航
- 物体导航（自动计算安全点）
- RL避障

### 4. 简单指令
```
"起飞"
"降落"
"向前飞3米"
"旋转60度"
```

---

## 简洁版（适用于简历）

### AgentFly：基于多模态LLM的智能无人机自主导航系统
**技术栈**：Python, ROS, LangChain, YOLOWorld, PPO强化学习, PyTorch, Gazebo

- 基于**LangChain**和**Qwen3-VL-Flash**构建多模态Agent，实现自然语言指令到飞行控制的端到端转换，任务成功率**85%+**
- 引入**任务规划器**（TaskPlanner）和**导航闭环反馈**（NavigationCallback）机制，搜索任务完成率从**40%**提升至**85%**（↑112%）
- 实现**空间障碍物感知**（v2.1），集成map_manager和onboard_detector服务，Agent能感知周围5米内障碍物，导航前自动碰撞检测，不可达路径率降低**50-80%**
- 实现**确定性逻辑代码化**和**提示词精简**，Agent代码从1145行优化到614行（↓46%），提示词长度减少**70%**（2000→600字符）
- 集成**YOLOWorld**开放词汇检测和RGB-D深度融合算法，实现物体3D定位，精度**±0.15m**，检测帧率**20-30 FPS**
- 集成**PPO强化学习**导航策略，实现速度级控制和动态避障，导航成功率**90%+**
- 开发**激光雷达模拟**（144点）和**3D占据地图**（0.1m分辨率）模块，支持实时建图和避障
- 设计**8个工具接口**和**安全管理层**，实现高度/距离/速度限制、异常恢复、任务中断等安全机制
- 完成**100+次**仿真测试，编写**3225行**Agent代码（含745行优化模块）和完整技术文档，支持仿真到真机的无缝迁移

---

## 技术细节补充（面试准备）

### 核心算法
- **ReAct推理**：Thought（分析状态）→ Action（选择工具）→ Observation（执行反馈）
- **PPO导航**：端到端速度控制，输入激光雷达+动态障碍物，输出3D速度向量
- **任务规划**：自动分解高级指令为子任务序列（搜索→[旋转×6, 导航, 旋转×6, ...]）
- **安全接近策略**：物体导航时自动计算安全点（物体前1.0m），避免碰撞

### 工程实践
- **面向对象设计**：Tool类封装，支持工具插件化
- **结构化输出**：Pydantic Schema确保LLM输出格式正确
- **降级解析**：Pydantic失败时使用正则提取，提高鲁棒性
- **图像质量检查**：自动检测模糊、黑屏、过曝
- **导航回调机制**：后台监控导航状态，完成后自动触发回调

### 性能优化
- **智能模式切换**：纯文本任务不启用视觉（节省成本）
- **异步导航**：set_goal后继续LLM循环，导航由PPO异步完成
- **GPU加速**：YOLO检测和PPO推理支持CUDA加速
- **提示词精简**：确定性逻辑代码化，提示词长度减少70%

### 技术难点与解决方案
1. **LLM输出不确定性** → 结构化输出 + 降级解析 + 安全校验
2. **飞行安全保障** → 多层安全机制（参数校验 + 距离限制 + 紧急停止）
3. **多模态成本优化** → 智能判断是否需要视觉输入
4. **导航闭环问题** → NavigationCallback后台监控 + 自动触发回调
5. **任务规划能力** → TaskPlanner显式规划器 + 进度追踪

## 许可证

MIT License
